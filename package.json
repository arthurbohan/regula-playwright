{
  "name": "auto-tests",
  "version": "1.0.0",
  "description": "test automation framework",
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "@types/node": "^24.3.0"
  },
  "scripts": {
    "ci test": "npx playwright test --headless=true",
    "test": "npx playwright test"
  }
}